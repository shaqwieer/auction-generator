# Pinned to the versions this codebase was built and tested against.

# Web
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.11.3
pydantic-settings==2.14.1
python-multipart==0.0.32
# Required by pydantic's EmailStr, which the auth schemas use.
email-validator==2.2.0

# Database
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.5

# Auth. bcrypt is held below 4.1: passlib 1.7.4 reads bcrypt.__about__, which
# newer releases removed, and the failure surfaces as a warning plus a broken
# hash rather than an import error.
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# Spreadsheet ingest
pandas==3.0.3
openpyxl==3.1.5

# Rendering. PyMuPDF carries the MuPDF Story engine (HarfBuzz + FriBidi) that
# does the Arabic shaping -- read docs/engine-decision.md before changing it.
PyMuPDF==1.28.0
pillow==12.2.0
qrcode==7.4.2
python-barcode==0.16.1

# NOTE: arabic-reshaper and python-bidi are deliberately absent. The client's
# brand fonts have no presentation-form cmap, so that pipeline emits NUL glyphs.
# tests/test_guards.py fails the build if either is imported by the renderer.
